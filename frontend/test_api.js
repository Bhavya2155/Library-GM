import axios from 'axios';

async function test() {
  try {
    // Need to login first to get the token! Or I can just check the db again. But I already know DB says 0.
  } catch (e) {
    console.error(e);
  }
}
test();
